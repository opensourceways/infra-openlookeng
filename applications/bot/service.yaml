---
apiVersion: v1
kind: Service
metadata:
  name: botservice
  namespace: bot
spec:
  externalTrafficPolicy: Cluster
  ports:
    - name: httpport
      protocol: TCP
      port: 80
      targetPort: 8888
      nodePort: 31202
  selector:
    app: botinfo
  type: LoadBalancer
